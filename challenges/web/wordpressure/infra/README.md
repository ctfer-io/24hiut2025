# Monter le challenge chez soi

```bash
docker build . -t ctferio/wordpressure
docker compose up -d
```

Ajouter `http://www.frontend-wannabe.com` à ntore fichier hosts et joindre le site sur le port `2600`.
